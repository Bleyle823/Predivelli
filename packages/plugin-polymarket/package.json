{
    "name": "@elizaos/plugin-polymarket",
    "version": "0.0.1",
    "main": "dist/index.js",
    "type": "module",
    "types": "dist/index.d.ts",
    "dependencies": {
        "@elizaos/core": "workspace:*",
        "@goat-sdk/adapter-vercel-ai": "0.2.10",
        "@goat-sdk/plugin-polymarket": "^0.3.0",
        "@goat-sdk/wallet-viem": "^0.3.0",
        "viem": "^2.0.0",
        "dotenv": "^16.0.0",
        "tsup": "8.3.5"
    },
    "devDependencies": {
        "vitest": "^1.0.0",
        "@types/node": "^20.0.0"
    },
    "scripts": {
        "build": "tsup --format esm --dts",
        "dev": "tsup --format esm --dts --watch",
        "test": "vitest run",
        "test:watch": "vitest watch",
        "test:coverage": "vitest run --coverage"
    }
}